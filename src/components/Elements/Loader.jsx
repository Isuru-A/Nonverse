import React from "react";
import {ClipLoader} from "react-spinners";

const Loader = () => (
    <div className="loader">
        <ClipLoader size={50} color={'#6951FF'}/>
    </div>
)

export default Loader